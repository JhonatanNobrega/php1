<?php

    /*
    |   IF DE UMA LINHA - SÓ ISSO, NADA MAIS!
    */

    $idade = 27;

    // Mostrar na tela de é meior ou menor de idade
    echo ( $idade >= 18 ) ? 'Maior de idade' : 'Menor de idade'; 

    // Amazenando valor na varialve de acordo com a condição passada
    $maiorDeIdade = ( $idade >= 18 ) ? true : false;

